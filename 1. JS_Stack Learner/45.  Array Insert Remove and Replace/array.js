let arr = [1, 2, 3, 4, 5, 6, 7, 8, 9,]

// arr.push(10)              // add element to last position

// arr.unshift(0)           // add element to first position

// arr.splice(3, 0, 6, 9)   //add element to specific position

// arr[2] = undefined       //remove element but leangth is same as before.

// arr.pop()                // remove element from last position.

// arr.shift()              // remove element from first position.

// arr.splice(3, 1)        // remove element from specific position.

console.log(arr) 